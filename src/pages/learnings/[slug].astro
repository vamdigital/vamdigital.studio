---
export function getStaticPaths() {
  return [
    {
      params: { slug: 'design-system-team-habit' },
      props: { destination: '/notes/design-system-team-habit' }
    },
    {
      params: { slug: 'learning-1' },
      props: { destination: '/notes/keeping-this-site-intentionally-small' }
    },
    {
      params: { slug: 'modernising-a-frontend' },
      props: { destination: '/notes/modernising-a-frontend' }
    },
    {
      params: { slug: 'pagination-that-feels-simple' },
      props: { destination: '/notes/pagination-that-feels-simple' }
    },
    {
      params: { slug: 'predictable-react-search-state' },
      props: { destination: '/notes/predictable-react-search-state' }
    },
    {
      params: { slug: 'typed-python-decorators' },
      props: { destination: '/notes/typed-python-decorators' }
    }
  ];
}

const { destination } = Astro.props;
return Astro.redirect(destination, 301);
---
